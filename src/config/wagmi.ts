// Re-export everything from the central chains config
// This file is kept for backward compatibility with existing imports
export { etherlinkShadownet as etherlink, SUPPORTED_CHAINS, DEFAULT_CHAIN } from "./chains";
